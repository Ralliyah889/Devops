apiVersion: v1
kind: Service
metadata:
  name: omni-backend-service
  namespace: omnicivilization
  labels:
    app: omni-backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: omni-backend
